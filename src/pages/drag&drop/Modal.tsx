import React from 'react';
import ModalWidget from '../../Components/ModalWidget';

function Modal() {
    return (
        <div>
            <ModalWidget/>
        </div>
    );
}

export default Modal;